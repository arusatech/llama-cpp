<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for binding a local TCP socket used by the in-process HTTP server -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
